<%= form_for [@emailable, @email] do |f| %>
  <p>
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </p>
  <p>
    <%= f.label :typ %><br />
    <%= f.text_field :typ %>
  </p>
  <p><%= f.submit "Submit" %></p>
<% end %>